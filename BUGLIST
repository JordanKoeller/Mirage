LightCurves generated by spark - are they correct? Can't seem to line them up with the view, and only half of them have any data inside!
One of these days I'll have to redo some of the code for View objects so they are independent of pyplot
Flag on peak-finder to say toss out dense regions. DONE
Improve on the symmetry calculation - just use a simple window rather than anything clever. Quasar radius is a natural window size. DONE
